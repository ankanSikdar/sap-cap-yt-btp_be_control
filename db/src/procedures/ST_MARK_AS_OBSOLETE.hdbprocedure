PROCEDURE "ST_MARK_AS_OBSOLETE" (
     IN IN_ASID NVARCHAR(36),
     IN IN_REASONCOMMENT NVARCHAR(50)
)
LANGUAGE SQLSCRIPT
SQL SECURITY INVOKER AS
BEGIN

UPDATE COM_CNTRL_ASSESMENTS
    SET ISOBSOLETE = TRUE,
    REASONCOMMENT = :IN_REASONCOMMENT
    WHERE ID = :IN_ASID;

END;